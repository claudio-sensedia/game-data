spring:
  rabbitmq:
    host: ${RABBIT_HOST}
    port: ${RABBIT_PORT}
  data:
    mongodb:
      database: "GAME-DATA"
      host: ${MONGO_HOST}
      port: ${MONGO_PORT}
server:
  port: 7777

match:
  exchange: matches
  result:
    queue: result
    key: result